# Ticket05 final panel verification

Implementation baseline: `27ce8f2`. The implementation is based on the approved v4 panel and the existing production poster/text/target modules; no prototype data or demonstration controls were merged into production.

- TDD: the production browser failed to expose the approved `分享海报` dialog before implementation. Public clipboard-result tests failed before updating image-download guidance and hiding internal combined-write reasons. Those checks now pass.
- `npm run check`: passed. Full `npm test`: 8 files, 70 tests passed. `verify-text-recovery.mjs`: passed after migrating only accessible control names/checkbox semantics. `verify-share-targets.mjs`: all 15 cases passed, retaining export/option locking and target consistency.
- `verify-final-panel.mjs`: final layout, 16px centered title, side-by-side image/combined actions, centered download under the preview, independent plain/Markdown actions and visible detail checkbox passed. All three close paths return to the entry; forward and reverse Tab stay inside the panel. 320px and390px layouts scroll to every action without horizontal panel overflow.
- Failure paths: cover failure leaves text available and disables image/combined/download; image-copy failure retains the preview and points to the download icon; combined-write failure copies the same ordinary text and explicitly says the image needs separate handling. Metadata failures and Markdown manual-source fallback are also retained by the ticket03 regression.
- Real Chromium user gestures, without clipboard mocks: plain-text clipboard matches the visible ordinary text; Markdown copy leaves that preview unchanged; PNG readback is1080×1440; combined copy offers `text/plain`, `text/html`, `image/png`. Actual paste into a controlled Chromium contenteditable received both the1080×1440 image and the ordinary text. This proves that representative receiver only, not universal paste compatibility.
- Actual downloaded PNG is1080×1440, with its filename and SHA-256 recorded inreport.json. macOS Vision decoded its QR to the expected canonical URL; seeqr-decoded.jsonl.
- Screenshots disable animations before capture to avoid intermediate opacity. Desktop light, dark CSS, cover failure,320/390px preview/actions were generated; light/dark/cover-failure and320px actions were visually inspected against approved v4. `dialog-light.png` and `dialog-dark-css.png` show only the production dialog, excluding the test-fixture page.
- The dark screenshot applies the planned appearance class directly: page/BewlyCat theme detection remains ticket06. Old A/B CSS and dev-mode labels were removed; the poster keeps its own palette.
- The existing close animation lifecycle is left for ticket07. Focus return is currently in the close callback; ticket07 must ensure an old close callback cannot steal focus from a newly opened panel.
- These controlled integration checks do not verify a live Tampermonkey installation, current Bilibili network endpoints, or the entire supported Chrome/Edge version matrix. The final installed build and remaining environment checks belong to whole-batch delivery.

Independent Standards/Spec review will be run by the primary agent after this scoped implementation commit.
