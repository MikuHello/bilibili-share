# 默认主题 v5 后续 discovery

Status: CLOSED — owner approved final prototype and proceeding to to-spec; implementation/spec approval separate

## 恢复基线

- 已读取临时 handoff.md、approval.md、redesign idea/investigation、父 idea/spec、CONTEXT.md 与 Matt 流程说明，并目视读取 poster.png。
- 当前 HEAD：4d1f355；既有未提交 idea.md、approval.md、investigation.md 与 prototype/ 保留。
- 第五版默认主题视觉已经批准；不重新设计，不继续旧工单，不合并原型代码。
- 本轮仍覆盖完整面板、原生入口与通用适配，没有把范围静默缩为仅实现海报。

## 本轮已确认（Q1–Q8）

主人已回答八项。以下为 discovery 决策，不是实现授权。

1. 面板：桌面左预览右操作、窄屏纵向。最初“次要选项折叠”的决定已被本轮反馈替代：“详细信息”开关常显，Markdown改为独立复制动作，复制海报/组合复制并列，下载改为海报旁图标，去掉冗余文案。保留全部既有功能。
2. 标题：第五版人工主副标题拆分不作为通用规则。保持原文顺序自动换行，在标题区域内有限缩字，仍溢出才尾部省略；完整标题留在分享文案。缩字下限和行数由边界原型验证后确认，不凭字符数猜主副标题。
3. 封面：非横版完整居中，以现有底色补空白，不裁切原图；海报其余结构遵循第五版。
4. 昵称：最多两行，再有限缩字/省略；二维码与统计位置稳定，详细文案保留完整昵称。
5. 分P/时间：最新反馈明确仅改变海报二维码与底部链接，不添加分P编号、分P标题或时间文字，不因标记开关替换视频标题/昵称/封面/统计。此决定替代此前“标题下方显示标记”。分享文案中的链接同步保持同一落点；原有开关联动规则保留。
6. 链接：主人明确地址栏链接应完整显示，允许换行。不得省略、截断或用溢出隐藏破坏完整网址。不再把咱原提案的“最多两行/高度绝对固定”当作已确认约束；仍保持第五版底部地址栏结构，具体行数、高度与可读字号须以最长有效分享链接验证。
7. 面板外观：跟随 B站/BewlyCat 页面实际浅深模式，使用统一面板配色，不逐项继承页面颜色；原生入口融入工具栏；导出海报保持第五版独立配色。
8. 无法识别外观：保持上次成功识别的外观；首次无法识别时使用浅色。不新增主题选择器，不直接拿系统偏好代替页面实际设置。此处未另行授权跨会话持久化主题缓存。

## 尚未解锁的分支

- Q1–Q8 已收回；继续收敛具体排版边界、面板各状态和独立视觉原型，不提前选定缩字下限或挤压其他区域。
- 封面实际失败已定位管理器域名授权层，见 runtime-followup.md；现可讨论不可恢复封面失败时的产品行为。最小修复候选未实施，可用本地素材不代表生产修复。
- 主题支持范围与回退已确认；真实切换、初始化和信号失效仍需运行时验证，不得使用源码检查冒充实测。
- mismatch 独立调查，不与封面缺失合并归因。
- 全部前沿收敛、主人确认共同理解后进入 to-spec；to-spec 中另核对测试接缝，规格与拆票审批之后才进入新工单。

## 本轮代码事实与验证边界

- 现有领域核心已承担快照到海报模型、分享目标、选项约束与文案格式化；可作为新规格优先沿用的测试接缝。
- 现有标题字号策略是 A/B 分支与字符数量阶梯，不是新默认主题的浏览器排版测量规则。
- 现有封面 adapter 测试主要覆盖 metadata 解析与 isUsableCover 尺寸规则，没有穿过实际 GM 请求、FileReader、Image.decode 与最终海报。
- 现有主题 token 测试检查 A/B 面板/入口类名绑定；无法证明跟随真实站点主题，不能作为新外观能力的验收。
- 海报导出经过 html-to-image；原型静态 PNG 与既有单元测试不能替代实际安装脚本导出和二维码落点验证。

独立调查证据分别记录于 discovery-runtime-2026-09-05.md 与 discovery-theme-2026-09-05.md（调查完成后生成）；未完成前不得引用为已核实事实。

## 主题源码调查返回后的决策状态

官方源码证据已记录在 discovery-theme-2026-09-05.md，真实浅深切换仍未验证。两个产品取舍已通过主人对 Q7/Q8 的“认可”确认：

- 支持主人正在使用的 BewlyCat 改写后页面外观；面板仅跟随有效浅深色模式，面板配色统一。原生入口融入工具栏的既定要求保持。
- 无法可靠识别时保持已识别外观，首次未知时浅色。不能以系统偏好冒充站点偏好。

Q7/Q8 不再重复询问。八项回答完成不等于整轮 discovery 结束：完整面板/入口与通用内容适配的视觉验证，以及封面、主题的实际链路证据仍待完成。

## 本轮验证交付与当前前沿

- validation-prototype/ 已完成单一方向的面板/入口与十种通用内容样例，真实浏览器布局/交互检查通过，十张 PNG 二维码均与可见网址匹配。详见其 README.md 与 evidence/。
- 原型实现标题64→48px、昵称34→30px的有限缩字；达到下限后省略；链接24px完整换行，极限样例页脚116→129px且画布不变。这些参数仍为待评议提案。
- 实装封面两次直接得到管理器 `@connect` 拒绝 → GM onerror → loadCover catch。实装和构建声明为 `*.hdslb.com`，官方域名写法 `hdslb.com` 可包含子域；修复候选留给新 spec/新工单，不在本轮修改。

现在可向主人提出：

1. 是否接受新增面板/入口、通用内容适配及上述边界参数？仅评议新增范围，不重开第五版视觉。
2. 封面仍无法取得时的产品策略：建议明确提示“封面暂时无法加载”，保留文案复制和重试，暂禁复制海报/下载PNG/组合复制，避免把英文占位作为成品。此建议尚未批准，当前原型的通用生成失败不代表该分支已实现。

主人答复后重新计算前沿；若没有剩余取舍，再请求共同理解确认并进入 to-spec。真实安装包修复后的成功链路、真实站点外观切换与剪贴板验收应保持明确验证要求，不能用静态原型替代。


## 面板 v1 评议与 v2 修订

主人表示“原型应该是比较符合我预期了，就是单说海报的话”，但明确认为操作面板问题较大。此反馈接受海报方向，不视为整套面板或 spec 批准。

已落实在 validation-prototype 的 v2：删除口号与冗余说明；标记开关改名“标记当前分P / 标记当前时间”（分享目标作用保留）；取消更多选项折叠；文案格式选项常显；复制海报与组合复制并列；海报旁提供下载图标。

Q10 已通过批注批准：封面无法取得时提示“封面暂时无法加载”，保留文案复制与重试，暂停复制海报、下载与组合复制。原型新增该状态并验证按钮可用性；不是生产修复。

当前待决策仅是新增面板 v2 的评议及可能的新反馈；之后再确认共同理解并进入 to-spec。Q10 不再重复询问。测试接缝按 to-spec 流程核对，真实修复/主题/剪贴板验收要求继续保留。


## 标记语义修订

主人明确：“标记当前分P和标记当前时间不会影响海报的内容，只会影响海报的二维码跟下方的那个链接。”

原型已删除分P/时间正文区域，不再占用标题高度。开关在当前内容样例上投影分享目标，不切换为其他内容样例。新旧目标的标题、署名、统计、编号保持一致，二维码/链接及分享文案中的链接统一变化。链接换行仍沿用完整显示规则。

已用真实浏览器比较长标题样例切换前后：文本字段相同、标题区域截图像素相同，二维码src和链接变化，无context-line节点，文案链接匹配。证据见 validation-prototype/evidence/marking/。此为原型验证，不是生产改动。


## 面板 v4：文案动作与原生视觉层级

主人要求“详细文案”改称“详细信息”；取消Markdown格式勾选，在文案右上角与复制文案并列增加复制Markdown。最新明确选择替代旧的格式模式，复制动作按当前详细信息选择产出对应格式。原型的组合复制使用普通文案，不由上次点击Markdown改变。

主人要求重新设计下载图标位置，并平衡标题/关闭按钮、背景、线条与B站原生界面的关系。独立现场采集见 native-panel-reference.md：官方模态对话框居中16px标题、透明底关闭图标、无标题分隔线；分享浮层是不同容器，不混同其交互。

v4 提案：16px居中标题、透明细叉、小圆角、轻阴影、无模糊的中性遮罩；移除左右两块底色和贯穿分隔线，采用统一面板底色与留白；下载图标置于海报下方居中。此为结合现场参考的设计提案，不声称复制了官方完整组件或已获视觉批准。

桌面浅深、320/390px、独立Markdown/普通文案复制、详细信息对两种格式的作用、封面失败时两种文案动作可用、原有标记联动均已通过原型浏览器验证，证据位于 validation-prototype/evidence/panel-v4/。复制动作仍为显式演示，不写真实剪贴板。海报画面和标记仅改落点的规则不变。

当前待评议为v4整体面板，不继续旧工单、不发布spec；主人确认视觉和共同理解后再进入to-spec。


## 最终确认与 discovery 关闭

主人明确确认当前最终版本并要求按Matt进入下一步。第四版面板和当前海报适配全部定稿，产品取舍前沿关闭；不再要求主人重复批准各轮已确认选项。历史“待评议/待批准”描述由 approval.md 的最终记录覆盖。

下一阶段 to-spec，沿用现有领域核心/浏览器副作用边界核对测试覆盖。原型归档独立分支，main保留决策与新spec；不将旧工单当新交付计划。


## to-spec 已合成

spec.md已发布为待主人审阅的规格，流程标签ready-for-agent不代表实现授权。接缝沿用父spec已明确与主人确认的领域核心/浏览器副作用边界，没有新增架构接缝；具体覆盖在新spec Testing Decisions列出。原型归档提交为587b5c07a4e1e9a47a3ae1f4d52cd82da480b1d1，归档工作树干净。新工单尚未创建，下一步为规格批准和拆票方案审批。
