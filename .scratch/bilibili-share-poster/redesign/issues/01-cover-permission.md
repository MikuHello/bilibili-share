# 01 — 恢复真实封面请求与图片导出

**What to build:** 更正封面域许可，构建并核对真实安装版本，在原故障视频走通请求、解码、预览到图片导出。此票可使用现有海报外观验收网络修复，不等待新设计。

**Blocked by:** None — can start immediately.

**Status:** implemented — review and final integrated real-manager validation pending

- [x] 封面声明采用 hdslb.com 所需范围，保留API/短链精确域，不使用全域通配。
- [ ] 在真实管理器中核对构建与安装版本，原故障视频完成请求、有效图片解码、预览及PNG导出。
- [ ] 保留无凭据的请求结果、类型/字节、解码尺寸与导出证据；不把静态声明检查或原型通过记为真实修复。
- [ ] 按批准规格的领域核心/浏览器副作用接缝执行适当TDD、类型检查和集成验证；完成code-review及提交，记录真实证据和未验证项。

**规格覆盖：** 56、57；11、43、55的基础链路。

**交付约定：** 本票属于已批准的默认主题与分享面板整批改版。遵守本计划spec与整批交付约定；可内部独立验证和提交，不向主人逐票交付或要求安装中间版本。全部完成后统一提供最终构建与验收结果。

## Comments

- 2026-09-05 实现：仅将构建头的 `*.hdslb.com` 更正为 `hdslb.com` 并重新构建；保留 `api.bilibili.com` 与 `b23.tv`，没有全域许可或白名单设置修改。审查基准为 `0b0890028055c0df7914ea97abc3dd303aaaea49`。
- RED 采用本计划 `runtime-followup.md` 已记录的真实管理器边界证据：原故障视频两次请求均在许可层被拒绝，随后出现缺封面预览。这是已有现场复现，未声称本实现会话重新运行该失败；不新增模拟管理器或仅断言字符串的单元测试来代替该边界。
- 自动化回归：适配器测试 5/5、全套测试 77/77、`npm run check`、`npm run build` 与 `git diff --check` 通过。这些结果只验证现有领域/适配器规则和构建，不构成真实封面 GREEN。
- 依照整批交付约定，真实安装版本核对、请求 HTTP/类型/字节、图片解码尺寸、预览与 PNG 导出由主会话在最终集成构建上统一完成。当前仍未验证，不将本票标为产品验收完成。两轴 code-review 由主会话协调，结果另记。
