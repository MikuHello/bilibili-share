import {copyCombinedPosterAndText,buildCombinedHtml} from './candidate-clipboard';
const text='组合粘贴验证 · 可编辑文字\nhttps://www.bilibili.com/video/BV1GJ411x7h7/';
const png=await fetch('./poster.png').then(r=>r.blob()).then(b=>new Promise<string>(resolve=>{const reader=new FileReader();reader.onload=()=>resolve(String(reader.result));reader.readAsDataURL(b)}));
for(const id of ['candidate','html'])document.getElementById(id)!.addEventListener('click',async()=>{try{if(id==='candidate'){document.getElementById('status')!.textContent=JSON.stringify(await copyCombinedPosterAndText(png,text));}else{await navigator.clipboard.write([new ClipboardItem({'text/html':new Blob([buildCombinedHtml(png,text)],{type:'text/html'}),'text/plain':new Blob([text],{type:'text/plain'})})]);document.getElementById('status')!.textContent='HTML-only API write succeeded';}}catch(e){document.getElementById('status')!.textContent=String(e)}});
document.getElementById('paste')!.addEventListener('paste',()=>setTimeout(()=>{const el=document.getElementById('paste')!;document.getElementById('result')!.textContent=JSON.stringify({images:el.querySelectorAll('img').length,text:el.innerText,editable:el.isContentEditable});},0));