# Ticket06 验证

下载PNG位于复制海报左侧，固定40px，与主动作同高。提示在动作组下方绝对定位，约120ms过渡；固定留出反馈空间，不随消息出现改变面板或动作坐标。成功3秒隐藏，失败保留；减少动态效果为0ms。

TDD：布局先因下载在预览下方失败；移动后通过。反馈测试先测得动作上移28.7px；改浮层后所有受测面板/按钮坐标相同。更新方向帮助单测先红后绿。

浏览器：desktop浅深截图、320/390px、焦点循环、Escape/关闭/遮罩、成功替换与消失、失败3.1秒仍可见、复制失败保留预览、封面失败保留文案、真实Chromium独立剪贴板和1080×1440下载全部通过。见panel/report.json。已人工查看320px截图，下载和复制同排，浮层在其下，不遮挡动作。

64单测、类型检查、构建、文本失败恢复3组、四独立导出均通过。

## Standards

独立审阅：0硬性违规，0可操作代码异味。

## Spec

审阅指出旧复制失败帮助仍指向海报下方，已在a9b9e68修复两处并验证。其余结论待返回。
