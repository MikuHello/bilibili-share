# 0.3.0 统一交付验收记录

日期：2026-09-07。Status: implementation verified; awaiting manual Tampermonkey installation verification。

## 产物与基线

- 单个正式安装包：[bilibili-share-poster.user.js](../../../../../dist/bilibili-share-poster.user.js)，版本0.3.0，195764字节。
- SHA-256：`c111e34eb75ad02de07c5b901cdc3016f886c298ae47d392e9f10257740b035b`。
- 实现工作树：`/Users/mikuhello/project/dev/bilibili-share-refinement`，分支`codex/usage-refinement`。
- 起点：delivery候选`d3fd199`；最终构建提交：`ec8ac80`。后续只补验收文档，不改变该产物。
- 原工作树及未提交设计资料保持原样；原型未整体合入生产；开发脚本不作为安装交付。
- 构建保留标准视频页范围与必要权限，已移除短链连接权限，无远程自动更新地址。

## 完成的变化

1. 所有输出统一canonical完整链接；分P/时间沿用快照语义，正文下一行直接接链接，av前缀小写。
2. 批准的B3海报：完整封面在上、标题在下、封面衍生模糊背景；标题有限缩字及省略，分隔线随标题；UP单行、四统计同排、图标恒定。
3. 标记更新保留静态海报和控件，仅更新二维码及链接；去除人为串行等待；同版本PNG和进行中的导出复用，失败可重试、过期任务不能回写。
4. 面板浅深颜色隔离宿主样式，工具栏入口协调；下载位于复制海报左侧；短促浮层反馈不挤布局，失败保留恢复路径。
5. 根据真实QQ粘贴结果移除组合复制，保留独立海报、普通文案、Markdown与下载。

## 最终构建验证

`npm test`：7文件、64测试通过。`npm run check`及`npm run build`通过。

`npm run test:browser`现在先构建，再让9个套件直接读取同一个`dist/bilibili-share-poster.user.js`。运行环境macOS、Chromium151.0.7922.34，GM网络/播放器边界受控。完整日志：[browser-run.txt](browser-run.txt)。

| 验证 | 结果与证据 |
| --- | --- |
| B3 | [18组实际PNG及几何报告](b3-poster/report.json)通过，包括4种标题×3画幅、极端统计/昵称、缺失与零、长目标、真实深色桌搭与暖色MV封面；2个导出背景像素对照确认配色随封面变化。18个PNG均由Apple Vision解码二维码。 |
| 目标 | [10场景](share-targets/report.json)通过，6个PNG解码与完整目标一致，无短链请求，包含分P/时间联动及重开重置。 |
| 竞态/缓存 | [9组](update-races/races.json)通过：乱序QR、关闭/导航、重复导出、失败重试、真实字体加载及目标/快照失效；2个实际PNG再次扫码。 |
| 面板 | [7组](final-panel/report.json)通过：320/390px、下载与复制同排、键盘焦点/3种关闭、浮层稳定及减少动态效果、失败恢复、原生Chromium独立剪贴板与1080×1440下载。已人工查看320px动作截图与实际PNG。 |
| 外观 | [受控浅深初始化和切换](appearance/browser-verification.md)通过，包含宿主h2/h3干扰、unknown/oklab、入口重挂、海报及PNG缓存不随模式改变。 |
| 生命周期及恢复 | 9组生命周期和3组文本恢复通过；初始/重试/目标请求中导航、未派发导航事件时操作、替换播放器、原播放状态恢复均覆盖。 |

真实封面样例仍使用明确的集成夹具身份/UP/统计；不是这些视频实时元数据的海报。方/竖图为合成边界样例。真实桌搭封面具有大面积暗部与高亮屏幕，暖色MV封面整体较低饱和；未声称穷尽所有封面或跨系统字体表现。Windows QQ未覆盖。

## 性能复核

同机同浏览器、1280×900、GM元数据和封面各固定40ms、每版3样本。旧版为ticket02 `92dc0dd`，对照数据见[ticket03](../ticket03/verification.md)；最终版见[after.json](update-performance/after.json)。这些时间包含Playwright动作及等待开销，不能视为真实网络或已安装Tampermonkey的速度保证，不计算p95。

| 阶段 | 优化前ms（3样本） | 最终0.3.0 ms（3样本） |
| --- | --- | --- |
| 打开 | 206.7 / 162.6 / 160.9 | 176.1 / 168.2 / 161.6 |
| 详情 | 146.4 / 249.9 / 252 | 226.9 / 229 / 226.8 |
| 标记 | 173.3 / 189.6 / 174.8 | 37.1 / 34.9 / 36.8 |
| 首次导出 | 111.5 / 110.2 / 109.8 | 106.8 / 104.4 / 104.2 |
| 重复导出 | 91.9 / 76.8 / 76.1 | 40.9 / 26.7 / 24.9 |

工作量证据更直接：标记由7次图像decode降到1次QR decode，零网络请求；详情不触发图像或网络工作且控件节点保持；重复导出从重新编码一次降为零编码，复用相同PNG。打开仍有2次请求，未承诺网络加速。3样本未观察到长任务；仍有小量recent-input layout-shift记录，不宣称全页面零位移。单独反馈测试的面板及动作坐标完全不变。

## 真实剪贴板决定

见[ticket05真实粘贴证据](../ticket05/verification.md)：标准PNG/text/plain/text/html在Edge默认contenteditable与macOS TextEdit可粘贴图片和独立文字，但macOS QQ只有图片；移除PNG表示仍未使QQ得到可编辑文字。依据批准规格的条件授权移除组合入口，没有增加平台专用代码。未发送任何消息，测试草稿已清理。

## Standards

整批`git diff d3fd199...ec8ac80`独立审阅：硬性规范违规0，尚未解决的可操作异味0。遵循批准设计与现有测试接缝；此前测量尺寸重复、目标更新方法命名建议已修复。Standards通过不代表实际安装验收完成。

## Spec

整批独立审阅：没有新增功能缺项、范围扩张或错误实现。保留1项已知外部验收缺口：最终构建尚未与Tampermonkey实际安装对应，不关闭此项。真实页面CSS探针和QQ/TextEdit证据均不能替代最终安装核对。

Standards：0未解决；Spec：0代码问题、1项外部安装验收待办。

## 唯一外部步骤与限制

最终本地`.user.js`访问被客户端接管/阻止，未观察到成功安装。尝试访问`edge://extensions/`时，浏览器工具安全策略明确拒绝，且禁止其他入口、CDP或间接方式绕过，因此停止代操作扩展管理页。没有更改扩展权限或其他设置，没有把安装尝试称作成功。

请主人在Tampermonkey中导入上述唯一脚本并核对0.3.0，然后刷新标准视频页。后续仍需核对管理器代码与SHA-256、真实页面浅深初始化/切换及复制下载。安装前的页面曾显示旧样式；没有最终版本逐字对应证据。第07票保留`awaiting-manual-install-verification`，第01–06票已完成。
