# 局部设计对照（throwaway）

A当前四等分白底；B推荐内容组等量间距＋透明二维码；C相同间距＋封面取色浅底。既有整版构图获认可，因此仅比较主人点名的细节，不制造整版差异。

运行：在该目录运行 `python3 -m http.server 8777 --bind 127.0.0.1`，访问 `http://127.0.0.1:8777/?variant=B`。底部箭头切换方案，顶部下拉切换数字。源码prototype.ts为临时渲染，禁止整包合并生产。

measure.mjs为本机浏览器几何与PNG观察脚本，路径固定在当前工作树。evidence/report.json记录3方案×4种数字的几何与Apple Vision扫码结果。全部12PNG可解码，所有图标54×54，数字无溢出。

A实测截图数字的图标起点步长190.34/190.36/190.34，而内容组尾部空白42.61/71.97/84.72；四个8888则40.66/40.67/40.66。B/C按内容分配，截图组间空白84.38/84.38/84.38；极端数组改整行统一有限缩字，图标不缩小。只验证本真实封面，不将其扩大为透明底二维码对所有图片都可靠。

已检查导出图，临时页面header选择器限定body>header，避免影响海报内部mast。A是对照；B/C尚未作为正式视觉实现获批。

## 第二轮局部调整

主人接受B方向，要求扫码观看靠近二维码，并与左侧统计形成同一水平信息行；明确不加中心小电视等图标。B/C把两侧信息纳入同一组，以统计行与扫码说明的中心线对齐，移除二维码底部额外padding和说明上margin；静区仍保留。1280和390视口实测两行中心差均0，见evidence/caption-alignment.json。12PNG重新扫码均通过。其余构图及信息字号不变。
