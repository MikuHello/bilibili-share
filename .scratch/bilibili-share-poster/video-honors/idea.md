# 视频荣誉标签与补充说明

Status: design confirmed — 原型已确认；spec与ticket-plan已整理待审阅，未开始生产实现。

## 本轮输入

主人明确满意0.3.4性能及既有功能。本轮新增/完善，不重新设计现有海报或重开已关闭问题。基线：codex/usage-refinement 2556337（构建8f7a114、0.3.4），工作树 /Users/mikuhello/project/dev/bilibili-share-refinement。

- 获取B站视频的热门/荣誉标签，保留原文，包括期数、名次和限定文字。截图示例：第389期每周必看、入站必刷98大视频、全站排行榜最高第4名。
- 标签显示在海报合适位置；主人提出四角、标题右下、logo与BV/av之间等候选，尚未选择。进入prototype比较后确认，禁止擅改正式视觉。
- 详细文案在收藏统计之后补充标签；默认简洁文案不加。具体换行形式与详细Markdown一致性首轮提问中。
- “当前播放位置不足1秒”属于补充说明：灰色按钮悬停展示原因，不插入常驻段落改变布局。其他无需显著提醒的信息遵循相同原则，尽量不影响海报或页面布局。
- 按Matt流程继续；可用handoff压缩并交接后续设计上下文。

## 已知代码事实

- src/ui/panel.ts renderShareOptions当前把不足1秒、分P无法识别两种说明作为p元素追加，确实参与布局。现有成功/失败浮层已独立布局，需要恢复操作的错误是否继续保留由首轮边界确认。
- src/bilibili.ts当前统一请求视频metadata，VideoApiData未定义荣誉字段；当前GenerationSnapshot与share-text尚无荣誉概念。
- 事实调查委派只读子任务：一方API字段、原文、多个荣誉和页面优先级、匿名访问与缺失/失败。调查输出暂存/tmp/bsp-honor-api-investigation.md，返回后整理为本目录技术证据。

## 当前设计树/前沿

1. API字段与多标签事实（调查中）→ 单/多标签显示取舍 → 视觉位置/密度原型。
2. 缺失/获取失败 → 首轮建议不阻止分享、省略标签、不显著报错，等待主人选择；是否有额外请求取决于调查。
3. 详细文案 → 首轮建议统计行后独立一行，详细plain和Markdown均包含、简洁均不包含，等待主人选择。
4. 补充说明 → 首轮建议悬停/键盘聚焦展示，触屏有点按入口；操作失败保留明确反馈，等待主人选择。
5. 前沿收敛、原型确认 → 主人确认共同理解 → to-spec → 规格与拆票批准 → to-tickets → 每票fresh implement/TDD/code-review → 统一交付。

原型仅用于选择标签位置及说明交互；不直接合入生产。不主动安装或发布。

## 第一轮答复

主人确认：标签缺失/失败时省略，继续正常分享；详细plain与Markdown在收藏统计行后另起一行写标签，简洁文案均不包含。主人选择“全部信息都收进提示里”，覆盖操作结果及错误，不仅静态说明。第二轮正在确认点击结果是否主动显示按钮旁提示，以及保留恢复入口。

一方API确认：现有view响应data.honor_reply.honor[].desc包含原始荣誉文案，无额外请求。BV1a3th65EQb标题匹配截图1，含第389期每周必看、全站排行榜最高第1名和热门收录；BV1MN4y177PB含入站必刷98大视频等五项。数组顺序不等于页面优先级，匿名页面访问受风控，未确认其优先级。第二轮待决定页面主标签/三类已有荣誉全部/API所有荣誉。完整证据见api-investigation.md。

## 第二轮答复

主人选择只展示B站页面主标签。API数组首项不能未经验证视作主标签；继续只读调查当前页面可见标签及关联当前BVID的读取接缝。

主人纠正提示理解：“我其实不是说这样的，我其实是说，这种信息直接悬浮显示就可以了”。按直接浮层显示、不占布局处理，不把所有信息强制藏入按钮悬停；不足1秒仍按最初要求灰色按钮悬停说明。后续原型展示常规说明/操作结果/错误浮层，保留原恢复动作，验证主人的准确意图。

## 主标签事实与视觉原型

主标签不确定性已收敛：两个实际页面及其加载的官方前端源码证实，页面映射荣誉数组首项（仅type 1/2/3）；不寻找后续受支持项；argue_info.argue_type为1/2时不展示。复用当前metadata可避免额外请求。详见primary-investigation.md；该证据对应当前前端构建，不是永久接口契约。

原型入口：`http://127.0.0.1:8784/?variant=C`，代码暂存本目录prototype/（throwaway，未进入生产构建）。A封面右上角；B标题右下；C Logo与编号之间（建议）。现有海报和面板由生产fixture生成，荣誉明确标注为模拟数据。下拉可切换三个原文或缺失，支持长标题、深色、详细文案、灰色时间戳悬停原因、成功/失败浮层。所有复制/下载行为是演示。

桌面三方案、长标题+最长样例、390px移动宽度及深色截图已检查，未见横向溢出或页面脚本错误。当前位置及提示尚待主人反馈；确定后归档原型到throwaway分支，进行共同理解确认、to-spec及拆票门禁。

## 第三轮：A 书签修订

主人选择A方向，并明确要求标签放大、向上抬到BV/av编号与封面右上角之间，可超过封面边界，类似更高一层的书签，突出荣誉信息。原型A已修订：字号27→38px，top159→116px，right79→48px；暖金书签底、左侧燕尾口、右下小折角，跨过封面上沿并略伸出其右沿。最长示例与编号仍有间隙，无重叠或横向溢出。此轮只更新原型，具体修订尚待视觉反馈。

2026-09-08主人确认“原型基本设计完成了”，要求按Matt Skills继续下一步。设计前沿视为收敛；A书签及原型归档到codex/prototype-video-honors（3e2d88eb30a859eec213a3e4ddafc3315676cc3e）。已整理spec.md与ticket-plan.md，沿用现有测试接缝，提出两个无行为阻塞关系的纵向切片。等待规格与拆分审阅后发布工单。
