# 建议拆票

Status: APPROVED — owner approved all three tickets and implementation

1. **普通文案模板接入实际分享流程**
   - Blocked by: None
   - 交付：共用变量/条件渲染能力，普通简洁/详细默认模板及开发者覆盖入口；实际面板完整预览与复制自定义文字，消除末行链接假设；配置错误回退默认文案。
   - 验证：默认输出兼容、自定义模板与条件块、零值/缺失/错误规则、链接换位的实际面板复制与预览。
   - 同步提供变量表和普通模板开发示例；既有 Markdown 行为在此票保持正常。

2. **Markdown 独立模板与四预设完整配置**
   - Blocked by: 01
   - 交付：Markdown 简洁/详细使用共用生成能力和独立模板；开发者可分别覆盖四种预设；处理 Markdown 文本转义与规范 URL；一类配置错误不影响另一类。
   - 验证：默认 Markdown 精确兼容、特殊标题字符、独立覆盖、详细开关与分P/时间切换、实际复制及单预设错误回退。
   - 完善开发文档与未来 GUI 复用入口说明。

3. **默认海报职责整理并保持导出行为**
   - Blocked by: None
   - 交付：默认布局拥有渲染/测量/目标更新细节，PNG 编码与缓存职责分离，现有面板继续完成海报预览、二维码更新、复制与下载。
   - 验证：当前视觉和尺寸、二维码目标、字体/目标变更缓存失效、过期更新保护及封面失败时文字可用。
   - 无第二布局或通用插件系统；更新维护说明。

建议按 01 → 02 → 03 执行，以减少共享面板文件的集成冲突；03 在功能上不依赖前两票，因此不虚构依赖。全部完成后进行整体回归并统一提供构建与验证记录。每票使用 fresh implement 上下文、TDD 和 code-review。

审批内容：规格范围、以生成结果及实际预览/复制/导出为测试入口，以及上述三票粒度与依赖。获批后发布逐票 Markdown 工单。
